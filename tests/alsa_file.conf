# 测试专用：由文件提供输入，不读取真实麦克风。
pcm.test_capture {
    type file
    slave.pcm { type null }
    infile { @func getenv vars [ ASR_TEST_PCM ] default "/nonexistent-asr-test.raw" }
    file "/dev/null"
    format "raw"
}
