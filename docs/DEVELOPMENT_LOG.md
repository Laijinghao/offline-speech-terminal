# 开发日志

## 2026-09-19：离线文件转写第一阶段

### 完成内容

1. 检查Windows和现有Ubuntu虚拟机环境，建立开发连接。
2. 从官方发布页获取固定版本的sherpa-onnx与SenseVoice INT8模型，记录来源、许可证和SHA256。
3. 在Windows与Ubuntu分别运行官方中文样例，验证模型及CPU运行环境。
4. 完成C11程序：WAV分块解析、输入校验、C API识别调用、文本输出、资源释放、分离计时与退出码。
5. 完成CMake及Windows构建脚本、Linux样例入口，补充依赖下载与校验脚本。
6. 完成WAV边界测试、8类命令行错误测试及真实模型测试；Ubuntu CTest四项通过。
7. 在隔离网络的Linux命名空间中验证识别不依赖在线服务。
8. 完成两段个人录音转换与识别，用户确认第二段文字正确。

### 一次排错过程

第一条录音的结果没有开头两个字。先检查格式，再给同一音频增加500ms前置静音做对照，结果未改变。截取开头回听后，用户确认原录音本身没有录到这两个字。因此不能归因于模型漏识别。

重新录音，开始和结束保留短暂静音后，第二条输出包含完整开头，用户确认整句正确。MP3转换仍有解码警告，作为未解决事项记录，没有隐去。

### 实测摘要

- Ubuntu官方短样例5.592秒，C程序识别约0.59秒，模型加载约1.50秒。
- 第二条个人录音10.032秒，识别约1.09秒，加载约1.42秒。
- 以上是单次观测，未形成准确率统计，不能代表开发板、长音频或噪声环境表现。

### 后续待办

- 补充不同语速、距离和背景噪声下的录音及人工标注。
- 优先使用标准PCM WAV录音，进一步检查录音工具导出的MP3警告。
- 在当前阶段结果稳定后再开展麦克风采集，不提前叠加界面、NPU或云服务。

个人音频及原始个人测试文件仅保留在本地；公开仓库保留开发方法、代码和汇总结果。
